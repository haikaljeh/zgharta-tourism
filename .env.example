# Zgharta Tourism App — Environment Variables
# Copy this file to .env and fill in your values.

# --- Client-side (React app) ---
# Supabase public URL and anon key (safe for client-side use)
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key
# Google Maps JavaScript API key (restricted to your domain)
REACT_APP_GOOGLE_MAPS_KEY=your_google_maps_key

# --- Server-side (scripts only) ---
# Supabase URL (same as above, without REACT_APP_ prefix)
SUPABASE_URL=https://your-project.supabase.co
# Supabase service role key — NEVER expose client-side
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
# Google API key with Places API enabled (for import scripts)
GOOGLE_API_KEY=your_google_api_key
